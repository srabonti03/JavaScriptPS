// Write a function that takes two numbers as parameters and returns their sum.

function sum(m, n) {
    return m + n;
}

    const sumArrow = (m, n) => m + n;
    console.log(sum(9, 6));
    console.log(sumArrow(5, 8));